/**
 * FIREBASE DISABLED - Using Custom OAuth Only
 * This file is kept for compatibility but Firebase is no longer initialized
 */

// Firebase completely disabled in favor of custom OAuth
console.log('🚫 Firebase configuration file loaded but Firebase is DISABLED - using custom OAuth only');

// Dummy exports for compatibility with existing imports
export const auth = null;
export const googleProvider = null;  
export const app = null;
export default app;